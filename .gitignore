# Secrets — wallet keys, API keys (Circle / Tavily / Nebius) must never be committed
.env
.env.*
!.env.example
*.pem
*.key
wallet.json
secrets/

# Dependencies
node_modules/
.venv/
__pycache__/
*.pyc

# Build / runtime
dist/
build/
.cache/
*.log

# Internal-only notes — references a private source project; never publish
internal/

# Local ledger / cache / state (regenerated at runtime)
data/*.db
data/*.db-*
data/*.sqlite*
data/*.local.json

# Clippy run state (regenerated at runtime; clippy.config/ is committed)
.clippy/

# OS / editor
.DS_Store
.idea/
.vscode/
