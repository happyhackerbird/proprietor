# Project supplemental lenses.
#
# Add entries here; each fills the Name / Question / Scope lens-entry shape.
# Additive-only: cannot disable or override clippy's core lenses.
# Malformed file = fail-loud halt at standardized-pass dispatch.

## (no project lenses yet)
